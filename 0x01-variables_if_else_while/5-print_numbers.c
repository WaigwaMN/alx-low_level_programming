#include <stdio.h>

/**
 * main - main function of the code
 *
 * Return: zero when uccessful
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
